export default async function HeartRate() {
  // This page is intentionally left blank. The layout handles the rendering of the
  // index route, and the nested routes are for route modals
  return null
}
